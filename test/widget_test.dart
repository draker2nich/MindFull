import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('App builds without crashing', (WidgetTester tester) async {
    // Placeholder — platform channels не работают в тестовом окружении.
    // Полноценные widget-тесты для экранов добавить после мока PlatformChannel.
    expect(true, isTrue);
  });
}